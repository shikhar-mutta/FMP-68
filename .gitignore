# Dependencies
node_modules/
.pnp
.pnp.js

# Monolith backup created during microservice refactor
backend.monolith.bak/

# Legacy monolith env (contains credentials, replaced by per-service env defaults in compose)
backend/.env.monolith-legacy

# Auto-generated by Docker bind-mount in online watch mode (the source
# of truth lives in frontend/setupProxy.online.js — copied in at startup)
frontend/src/setupProxy.js

# Build output
dist/
build/
*.tsbuildinfo

# Environment files — NEVER commit real credentials
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Prisma
backend/prisma/migrations/

# Logs
*.log
npm-debug.log*
package-lock.json

# Test coverage
coverage/
**/coverage/
.nyc_output/
lcov.info
*.lcov

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/

# Ignored top-level directories
Movie-Booking-Service-Frontend/
Movie-Booking-System-Microservices/
docs/

# LaTeX build artifacts (report/)
report/*.aux
report/*.fdb_latexmk
report/*.fls
report/*.out
report/*.toc
report/*.lof
report/*.lot
report/*.synctex.gz
report/*.bbl
report/*.blg
report/*.nav
report/*.snm
report/*.vrb

Demo_Video.mp4

# (Previously gitignored — vault.yaml now uses kubectl-set-env injection
# for FMP_JWT_SECRET / FMP_GOOGLE_CLIENT_* and is safe to commit.
# The Jenkins fmp-infra pipeline patches the deployment before seed.sh
# runs, so no real secrets are baked into the manifest.)