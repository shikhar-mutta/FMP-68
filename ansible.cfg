[defaults]
# Roles path relative to project root (where ansible-playbook is invoked)
roles_path     = ansible/roles
inventory      = ansible/inventory/hosts.ini
host_key_checking = False
retry_files_enabled = False

[privilege_escalation]
become       = False
become_method = sudo
