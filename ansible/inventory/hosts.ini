# FMP-68 — Ansible Inventory
# For local deployment (Jenkins runs on the same machine as Docker)
[local]
localhost ansible_connection=local

[fmp68_servers]
localhost ansible_connection=local

[fmp68_servers:vars]
ansible_python_interpreter=/usr/bin/python3
