# Ansible env.j2 template — not used in current setup
# (env vars are passed directly via docker-compose envFrom/environment)
# Kept for reference if switching to .env file injection approach.

PORT={{ service_port }}
DATABASE_URL={{ database_url | default('mongodb://mongodb:27017/fmp68?replicaSet=rs0&directConnection=true') }}
JWT_SECRET={{ jwt_secret }}
NODE_ENV=production
