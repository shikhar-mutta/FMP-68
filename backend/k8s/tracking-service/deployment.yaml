apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  namespace: fmp
  labels: { app: tracking-service }
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  selector:
    matchLabels: { app: tracking-service }
  template:
    metadata:
      labels: { app: tracking-service }
    spec:
      containers:
        - name: tracking-service
          image: fmp-tracking-service:local
          imagePullPolicy: Never
          ports:
            - { name: ws, containerPort: 4004 }
          env:
            - { name: SERVICE_NAME, value: tracking-service }
            - { name: PORT,         value: "4004" }
          envFrom:
            - configMapRef: { name: fmp-config }
            - secretRef:    { name: fmp-secret }
          resources:
            requests: { cpu: "150m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /health, port: 4004 }
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /health, port: 4004 }
            initialDelaySeconds: 5
            periodSeconds: 5
