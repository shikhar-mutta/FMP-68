---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: follow-service
  namespace: fmp68
  labels:
    app: follow-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: follow-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: follow-service
    spec:
      containers:
        - name: follow-service
          image: shikhar68/fmp68-follow-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4004
          envFrom:
            - configMapRef:
                name: fmp68-config
          env:
            - name: PORT
              value: "4004"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: fmp68-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /follow-requests/health
              port: 4004
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: follow-service
  namespace: fmp68
spec:
  selector:
    app: follow-service
  ports:
    - protocol: TCP
      port: 4004
      targetPort: 4004
  type: ClusterIP
