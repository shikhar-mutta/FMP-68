apiVersion: v1
kind: Secret

metadata:
  name: follow-request-secret
  namespace: fmp

type: Opaque

stringData:
  DATABASE_URL: "${DATABASE_URL}"
  GOOGLE_CLIENT_ID: "${GOOGLE_CLIENT_ID}"
  GOOGLE_CLIENT_SECRET: "${GOOGLE_CLIENT_SECRET}"
  GOOGLE_CALLBACK_URL: "${GOOGLE_CALLBACK_URL}"