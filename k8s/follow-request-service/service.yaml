apiVersion: v1
kind: Service

metadata:
  name: follow-request-service
  namespace: fmp

spec:
  selector:
    app: follow-request-service

  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003