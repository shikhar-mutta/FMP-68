apiVersion: v1
kind: Service

metadata:
  name: gateway-service
  namespace: fmp

spec:
  type: NodePort

  selector:
    app: gateway-service

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000