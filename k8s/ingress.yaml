apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: gateway-ingress
  namespace: fmp

spec:
  ingressClassName: nginx

  rules:
    - host: fmp.local

      http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: gateway-service

                port:
                  number: 3000